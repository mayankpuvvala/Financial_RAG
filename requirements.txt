# Data acquisition
sec-edgar-downloader>=5.0.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0
pandas>=2.0.0

# Data models & config
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Tokenization
nltk>=3.8.0
tiktoken>=0.7.0

# Vector store
qdrant-client>=1.9.0
fastembed>=0.3.6   # ONNX dense/sparse embeddings AND cross-encoder reranking — no PyTorch

# LLM — Groq free tier
groq>=0.9.0

# API layer
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Evaluation (RAGAS + judge LLM wrappers)
ragas>=0.2.0
datasets>=2.20.0
langchain-groq>=0.1.0
langchain-huggingface>=0.0.3

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
loguru>=0.7.0
